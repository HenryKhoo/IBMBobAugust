# NOTE: BACKEND_MODE has been removed. It documented a "mock" mode that was
# never implemented (no backend/app/services/providers.py exists, and nothing
# branched on the value) — its only effect was making GET /health report a
# healthy "mock" backend on a deployment that could not serve a single
# request. The watsonx.ai and Zilliz credentials below are all required.

# IBM watsonx.ai
WATSONX_API_KEY=
WATSONX_PROJECT_ID=
WATSONX_URL=https://eu-de.ml.cloud.ibm.com
WATSONX_EMBEDDING_MODEL_ID=ibm/granite-embedding-278m-multilingual
WATSONX_INSTRUCT_MODEL_ID=ibm/granite-4-h-small
WATSONX_INSTRUCT_MODEL_FALLBACK_ID=meta-llama/llama-3-3-70b-instruct

# Gemini — when set, this becomes the PRIMARY model for /ask generation,
# with both watsonx models above demoted to an automatic two-tier fallback
# chain (watsonx's own rate limit was being hit too often to trust as the
# first attempt). Leave GEMINI_API_KEY blank to keep watsonx as primary,
# unchanged. GEMINI_API is also accepted as an alternate name for this var
# (e.g. if that's what a deployment's dashboard already has set). Get a key
# at https://aistudio.google.com/apikey. Embeddings/ingestion are
# unaffected either way — they stay watsonx/Granite-only, see
# app/services/watsonx.py.
GEMINI_API_KEY=
GEMINI_INSTRUCT_MODEL_ID=gemini-2.5-flash

# Zilliz Cloud (managed Milvus)
ZILLIZ_URI=
ZILLIZ_TOKEN=
ZILLIZ_COLLECTION_NAME=the_north_star

# Server
PORT=8000
